#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='swanintro',
      version='0.0.1',
      description='Information popup for SWAN',
      include_package_data=True,
      packages=find_packages(),
      zip_safe=False,
      install_requires=[
      ],
  )