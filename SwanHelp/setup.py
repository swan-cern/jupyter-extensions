#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='swanhelp',
      version='0.0.1',
      description='Help for SWAN',
      include_package_data=True,
      packages=find_packages(),
      zip_safe=False,
      install_requires=[
      ],
  )